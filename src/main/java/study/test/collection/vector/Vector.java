package study.test.collection.vector;

public class Vector {
    /*
    * ArrayList의 구버전
      - ArrayList와 동일한 내부 구조를 가지고 있다.
      - 모든 메소드가 동기화 되어있고, 잘 쓰이지 않음

      - 기본 생성자로 Vector 객체를 생성
        List<E> list = new Vector<E>();

      - ArrayList와는 달리 동기화된 메소드로 구성되어 있어 멀티 스레드환경에서 동시에 메소드들을 실행할 수 없고,
        하나의 스레드의 실행이 완료되어야만 다른 스레드에서 실행할 수 있다.
    * */
}
