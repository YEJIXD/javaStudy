package study.test.collection;

public class Stack_Queue {
    /*
    * Stack : LIFO(후입선출, Last IN First OUT) 자료구조
       · LIFO 자료구조를 구현한 클래스

       · Stack 객체 생성
           Stack<E> stack = new Stack<E>();
           
       · Stack 클래스 주요 메서드
         - push(E item) [return type : E] : 주어진 객체를 스택에 추가
         - peek()       [return type : E] : 스택의 최상단(top) 객체를 가져온다 (객체를 제거하지 않음)
         - pop()        [return type : E] : 스택의 최상단(top) 객체를 가져온다 (가져온 객체를 제거함)

    * Queue : FIFO(선입선출, First IN First OUT) 자료구조
       · FIFO 자료구조를 구현한 클래스

       · Queue 객체 생성
           Queue<E> queue = new Queue<E>();

       · Queue 클래스 주요 메서드
         - offer(E e)   [return type : boolean] : 주어진 객체를 추가
         - peek()       [return type : E]       : 가장 처음 추가된 객체(front)를 가져온다 (객체를 제거하지 않음)
         - pop()        [return type : E]       : 가장 처음 추가된 객체(front)를 가져온다 (가져온 객체를 제거함)
    * */
}
